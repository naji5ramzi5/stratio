# StratoCrypto — نظام التداول المتقدم بالعملات الرقمية

## نظرة عامة

StratoCrypto هو نظام تداول آلي متكامل للعملات الرقمية (Cryptocurrency) مبني بلغة Python، يستهدف استغلال **فرصة القيمة النسبية (Relative Value / Statistical Arbitrage)** عبر تداول أزواج العملات (Pairs Trading) بدلاً من التنبؤ بالاتجاه.

---

## الفلسفة الاستراتيجية

### المشكلة
التنبؤ باتجاه سعر العملة (هل يصعد أو ينزل) هو شبه مستحيل على المدى القصير — الدقة تتراوح بين 45-48% (أقل من رمي العملة). كل محاولات بناء نماذج ML للتنبؤ بالاتجاه فشلت بشكل متكرر.

### الحل
بدلاً من التنبؤ بالاتجاه، نستغل حقيقة أن **أزواج معينة من العملات تتحرك معاً على المدى الطويل** (تتحرك بنفس الاتجاه). عندما يبتعد أحدهم عن الآخر بشكل غير طبيعي، نراهن أنهما سيعودان للتحرك معاً — وهذا احتمال أعلى بكثير من 50%.

### مثال مبسط
- البيتكوين والإيثريوم يتحركان معاً غالباً
- فجأة الإيثريوم يصعد 10% والبيتكوين ما يتحرك
- نشتري البيتكوين (الرخيص) ونبيع الإيثريوم (الغالي)
- لما يعودان لبعض — نغلق الصفقة ونجمع الربح

---

## هيكل المشروع

```
stratocrypto/
├── advanced_bot.py          # البوت الرئيسي — يشغل كل شي تلقائياً
├── pair_paper.py            # دفتر تداول الأزواج الورقي
├── stat_arb.py              # محرك التحليل الإحصائي للأزواج
├── regime_predictor.py      # متنبئ النظام والتقلب (NEW)
├── portfolio_manager.py     # مدير المحفظة الذكي
├── market_data.py           # أنبوب البيانات المشترك
├── prediction_tracker.py    # متتبع التوقعات مع قفل ذري
├── regime_gate.py           # بوابة فلترة النظام
├── paper_trading.py         # محرك التداول الورقي أحادي العملة
├── launch_bots.py           # لاuncher موحد للبوتات
├── predictor.py             # متنبئ الاتجاه (مُستخدم للتقارير فقط)
├── ml_trainer.py            # تدريب نماذج ML الاتجاهية (مُهمل للتداول)
├── settings.py              # الإعدادات الموحدة
├── scan_smart.py            # ماسح الأزواج الذكي
├── scan_wide_inc.py         # ماسح تزايدي واسع
├── scan_fast.py             # ماسح سريع (يجلب البيانات مرة واحدة)
├── scan_multitf.py          # ماسح متعدد الأطر الزمنية
├── verify_tracker.py        # أداة التحقق من التوقعات
├── data_loader/             # محملات البيانات
│   └── binance_ohlcv.py     # محمل Binance الأساسي
├── tests/                   # الاختبارات
│   ├── test_pair_paper.py   # 11 اختبار لدفتر الأزواج
│   ├── test_regime.py       # 6 اختبارات لمتنبئ النظام
│   ├── test_stat_arb.py     # اختبارات محرك الأزواج
│   ├── test_regime_gate.py  # 15 اختبار للبوابة
│   ├── test_tracker.py      # 8 اختبارات للمتتبع
│   └── test_calibration.py  # 4 اختبارات للمعايرة
├── models/                  # النماذج المدربة
│   └── calibration.json     # جدول المعايرة
├── pairs_active.json        # الأزواج النشطة الحالية (15 زوج)
├── pairs_robust.json        # الأزواج المتينة (multi-window)
├── pairs_db.json            # قاعدة بيانات كل الفرص (82 فرصة)
├── portfolio.json           # المحفثة الحالية (12 موقع)
├── market_cache.json        # ذاكرة التخزين المؤقت للبيانات
├── paper_pairs.json         # صفتك الأزواج الورقية المفتوحة
├── ARCHITECTURE_REVIEW.md   # مراجعة معمارية مفصلة
└── requirements.txt         # الاعتماديات
```

---

## المكونات الأساسية

### 1. محرك التحليل الإحصائي (stat_arb.py)

**الوظيفة:** يحدد أي أزواج العملات تتحرك معاً (متكاملة إحصائياً Cointegrated).

**كيف يعمل:**
1. **اختبار Engle-Granger:** يختبر إذا كان هناك علاقة توازن طويلة المدى بين عملتين
2. **حساب Hedge Ratio:** يحدد كم وحدة من العملة B لكل وحدة من A لإنشاء "Spread" ثابت
3. **اختبار Z-score:** يقيس كم ابتعد السعر عن متوسطه (بالوحدة المعيارية)
4. **اختبار Half-life:** يقيس كم يستغرق السعر للعودة للمتوسط (بالأيام)
5. **Walk-forward Backtest:** يختبر الاستراتيجية على بيانات تاريخية بشكل سببي (لا تسريب بيانات)

**الأوامر:**
```bash
python stat_arb.py --pair BTCUSDT LTCUSDT           # اختبار زوج واحد
python stat_arb.py --scan                           # مسح كون واسع
python stat_arb.py --rescan                         # مسح + إثبات OOS
python stat_arb.py --monitor --pair BTCUSDT LTCUSDT # إشارة حية
```

**المخرجات:** `pairs_universe.json` (أزواج معتمدة) و `pairs_evidence.json` (أدلة OOS)

---

### 2. دفتر تداول الأزواج (pair_paper.py)

**الوظيفة:** ينفذ صفتك أزواج ورقية (بأموال وهمية) بناءً على Z-score.

**آلة الحالة (State Machine):**
```
FLAT (لا موقف)
  ← إذا |Z| ≥ 2.0 و 4h يوافق ← OPEN (موقف مفتوح)
  
OPEN (موقف مفتوق)
  ← إذا |Z| ≤ 0.5 ← CLOSE (عودة للمتوسط = ربح)
  ← إذا |Z| ≥ 3.5 ← CLOSE (وقف خسارة = cointegration تنكسر)
  ← إذا bars_held ≥ 168 ← CLOSE (انتهاء الوقت = 7 أيام)
```

**الميزات:**
- **Multi-TF Confirmation:** لازم إطار 1h و 4h يوافقوا على نفس الاتجاه
- **Stop-loss:** يقفل إذا السعر واصل الابتعاد (Z > 3.5)
- **Position Sizing:** حجم المركز يعتمد على قوة الإشارة:
  - Z = 2.0 → 3% من رأس المال
  - Z = 3.0 → 9% من رأس المال
  - Z ≥ 4.0 → 15% (حد أقصى)
- **Portfolio Cap:** مجموع التعرض ما يزيد عن 50%
- **Causal Baseline:** يحسب المتوسط/الانحراف على الماضي فقط (بدون تسريب)

**حساب الربح:**
```
الربح = الاتجاه × (تغيّر السpread) - 2 × تكلفة التداول
```

**الأوامر:**
```bash
python pair_paper.py --pair BTCUSDT LTCUSDT    # خطوة واحدة
python pair_paper.py --stats                    # إحصائيات المحفظة
```

---

### 3. متنبئ النظام (regime_predictor.py)

**الوظيفة:** يحدد حالة السوق الحالية (اتجاهي؟ انطوائي؟ عنيف؟)

**الميزات المستخرجة (13 ميزة):**
- **vol_5/20/50:** التقلب على 5/20/50 شريط
- **vol_ratio:** نسبة التقلب الحالي للتاريخي (هل يتسارع؟)
- **autocorr:** الارتباط الذاتي للعوائد (موجب = اتجاهي، سالب = انطوائي)
- **directional_dx:** نسبة الشرائح في الاتجاه السائد (0.5 = متوازن، 1.0 = اتجاهي)
- **var_ratio:** نسبة التباين (أقل من 1 = انطوائي، أعلى من 1 = اتجاهي)
- **vol_of_vol:** تقلب التقلب (هل السوق مضطرب؟)
- **atr_ratio:** نسبة المدى الحقيقي
- **vol_trend:** اتجاه الحجم
- **return_skew:** انحراف العوائد (ذيل سمين = أحداث متطرفة)

**التصنيف:**
- **TRENDING (اتجاهي):** autocorr > -0.08 و dx > 0.65
- **MEAN_REVERTING (انطوائي):** autocorr < -0.05 أو var_ratio < 0.8
- **VOLATILE (عنيف):** vol_20 > 1.0

**النتيجة (0-100):**
- **80+:** مثالي للأزواج → TRADE
- **50-79:** مناسب بحذر → CAUTION
- **أقل من 50:** غير مناسب → SKIP

---

### 4. مدير المحفظة (portfolio_manager.py)

**الوظيفة:** يوزع رأس المال على الفرص المتاحة باستخدام Kelly Criterion.

**خوارزمية الحجم:**
```
Kelly Size = (متوسط العائد) / (تباين العائد) × 0.25
```
- **fraction = 0.25:** Quarter-Kelly للأمان (نصف Kelly المحافظ)
- **Cap = 5%:** كل زوج لا يزيد عن 5% من رأس المال
- **Max Total = 60%: ** لا يزيد التعرض الكلي عن 60% (40% كاش احتياطي)

**الترتيب:** الفرص تُرتب حسب: `win_rate × log(Sharpe) × log(trades)`

**المخرجات:** `portfolio.json` — 12 موقع موزع على 12 زوج

---

### 5. البوت الرئيسي (advanced_bot.py)

**الوظيفة:** يشغل كل العمليات تلقائياً كل 20 دقيقة.

**الحلقة الرئيسية (كل 20 دقيقة):**
1. مسح السيولة (Order Book) — يكتشف الجدران والسبوفينغ
2. مسح التوقعات — يتوقع تحركات > 3% (تقرير فقط)
3. مسح الأزواج — يفتح/يقفل صفتك أزواج
4. تحقق من التوقعات القديمة — يتعلم من الأخطاء
5. تقرير يومي — يرسل Telegram

**التكامل مع Telegram:**
- **Bot 1:** إشارات AI + فتح/إغلاق الأزواج
- **Bot 2:** تنبيهات السيولة
- **Bot 3:** التوقعات والتقارير اليومية

---

### 6. بوابة النظام (regime_gate.py)

**الوظيفة:** تمنع التداول ضد الاتجاه العام للسوق.

**المنطق:**
- **PASS:** الإشارة مع الاتجاه → تداول
- **COUNTER_TREND:** الإشارة ضد الاتجاه → تُقارَر ولا تُتداول (gate_blocks=False)
- **NEUTRAL:** التوقع قريب من صفر → لا تداول
- **CONDITIONAL:** السوق مسطح وثقة عالية → تداول بحذر

**الدليل التجريبي:** اختبار A/B أثبت أن البوابة الصارمة تضر:
- بدون بوابة: 50.2% دقة
- مع بوابة: 40.0% دقة (أسوأ!)

**الوضع الحالي:** مُحوّل لوضع التقرير فقط (لا يمنع).

---

### 7. أنبوب البيانات (market_data.py)

**الوظيفة:** يجلب البيانات من Binance ويخزنها مؤقتاً (TTL Cache).

**الميزات:**
- **TTL Cache:** كل نوع بيانات له وقت صلاحية:
  - الأسعار: 60 ثانية
  - شرائح 1h: 5 دقائق
  - شرائح 5m: 2 دقيقة
- **Thread-safe:** قفل بين الخيوط
- **Fallback:** لو الكاش فشل، يجلب مباشرة من Binance

**الفائدة:** يقلل استدعاءات API بنسبة 80% (مهم لتجنب Rate Limiting).

---

## الفرص المكتشفة (82 فرصة)

### عبر 5 أطر زمنية:

| الإطار | الفرص | أفضل شارب | متوسط الفوز |
|---|---|---|---|
| **5m** (5 دقائق) | 63 | 28.7 | 19% |
| **15m** (15 دقيقة) | 10 | 7.1 | 7% |
| **1h** (ساعة) | 58 | 10.2 | 38% |
| **4h** (4 ساعات) | 94 | 53.3 | 26% |
| **1d** (يومي) | 41 | 55.7 | 52% |

### أقوى 12 موقع في المحفظة:

| الزوج | الإطار | شارب | فوز | تعرض |
|---|---|---|---|---|
| CRV/FLOKI | 5m | 23.9 | 100% | $500 (5%) |
| 1000SATS/SHIB | 1d | 55.7 | 83% | $500 (5%) |
| 1000SATS/ATOM | 1d | 33.8 | 100% | $500 (5%) |
| FLOKI/SUI | 1d | 31.0 | 86% | $500 (5%) |
| BONK/XRP | 4h | 16.9 | 60% | $500 (5%) |
| CRV/LDO | 4h | 53.3 | 42% | $500 (5%) |
| SEI/SHIB | 4h | 16.9 | 55% | $500 (5%) |
| DOGE/SUI | 4h | 24.3 | 45% | $500 (5%) |
| 1000SATS/DOT | 1h | 6.9 | 62% | $500 (5%) |
| 1000SATS/SOL | 1h | 8.1 | 57% | $500 (5%) |
| 1000SATS/AAVE | 1h | 9.7 | 54% | $500 (5%) |
| DOGE/WLD | 1d | 13.6 | 50% | $500 (5%) |

---

## كيفية الاستخدام

### التثبيت

```bash
pip install numpy pandas scipy statsmodels scikit-learn requests python-dotenv
```

### التشغيل السريع

```bash
# 1. شغّل كل شي (موصى)
python launch_bots.py

# 2. أو يدوياً:
python advanced_bot.py          # البوت الرئيسي
python dashboard_server.py      # لوحة التحكم (localhost:5000)
```

### المسح والتحديث

```bash
# مسح أزواج جديدة (كل أسبوع)
python scan_smart.py            # مسح 42 رمز
python scan_wide_inc.py         # مسح تزايدي واسع

# تحديث المحفظة
python portfolio_manager.py      # يوزع رأس المال

# التحقق من النظام
python regime_predictor.py      # حالة السوق الحالية
```

### المفاتيح المطلوبة (للتداول الفعلي)

في ملف `.env`:
```
BINANCE_API_KEY=your_key
BINANCE_SECRET=your_secret
TELEGRAM_PREDICTION_TOKEN=bot_token
TELEGRAM_LIQUIDITY_TOKEN=bot_token
TELEGRAM_PREDICTOR_TOKEN=bot_token
BOT4_TOKEN=master_bot_token
```

---

## كيف تستفيد من المشروع

### 1. كتاجر فردي (مبتدئ)
```bash
python launch_bots.py
```
- خليه يشتغل وتلقّى التنبيهات على Telegram
- كل ما يجيك "🔗 Pairs OPEN" افتح الصفقة يدوياً على بينانس
- كل ما يجيك "✅ Pairs CLOSE" قفل الصفقة

### 2. كتاجر متقدم
- شغّل `scan_multitf.py` كل يوم — يمسح كل الأطر الزمنية
- استخدم `regime_predictor.py` لمعرفة إذا السوق مناسب
- استخدم `portfolio_manager.py` لتوزيع رأس المال
- فعّل التداول التلقائي على بينانس (يحتاج API keys)

### 3. كباحث/مطور
- `stat_arb.py --rescan` — يثبت فرص جديدة
- `scan_fast.py` — مسح سريع (يجلب البيانات مرة واحدة)
- `verify_tracker.py` — يتحقق من دقة التوقعات
- كل الكود موثّق ومع اختبارات (71 اختبار)

---

## الأداء التاريخي

### الأزواج (OOS — بيانات تاريخية)

| المقياس | القيمة |
|---|---|
| الأزواج المتينة | 82 |
| متوسط شارب | 4.2 |
| متوسط الفوز | 52% |
| إجمالي العائد التاريخي | 15-300% (حسب الزوج) |

### التوقعات الاتجاهية (مُهملة)

| الإطار | الدقة |
|---|---|
| 6h | 48.8% |
| 12h | 46.4% |
| 24h | 47.5% |
| 48h | 45.5% |

**الخلاصة:** التوقعات الاتجاهية ميتة — لا تستخدمها للتداول.

---

## المخاطر والتحذيرات

1. **البيانات التاريخية ≠ المستقبل:** كل النتائج OOS (على بيانات ماضية). الأداء الفعلي قد يختلف.
2. **التسوية (Slippage):** الحسابات تفترض التنفيذ بالسعر الحالي — الواقع مختلف.
3. **السيولة:** بعض الأزواج (FLOKI, BONK) قليلة السيولة — صعب التنفيذ بحجم كبير.
4. **الانقطاع الإحصائي:** Cointegration ممكن تنكسر (وهذا ليش عندنا وقف خسارة).
5. **معدل الاستدعاء (Rate Limit):** Binance يحد الاستدعاءات — أنبوب البيانات يساعد لكن انتبه.

---

## الاختبارات (71 اختبار)

```bash
python -m unittest discover -s tests
```

| الاختبارات | العدد | الحالة |
|---|---|---|
| دفتر الأزواج | 11 | ✅ |
| متنبئ النظام | 6 | ✅ |
| محرك الأزواج | 10 | ✅ |
| بوابة النظام | 15 | ✅ |
| المتتبع | 8 | ✅ |
| المعايرة | 4 | ✅ |
| ML Trainer | 11 | ✅ |
| **المجموع** | **71** | **✅ كلها ناجحة** |

---

## التحديثات الأخيرة

- **regime_predictor.py (NEW):** متنبئ النظام والتقلب — بديل عن التنبؤ بالاتجاه الميت
- **scan_fast.py:** مسح سريع (يجلب البيانات مرة واحدة بدل 861 مرة)
- **scan_multitf.py:** مسح 5 أطر زمنية دفعة واحدة
- **portfolio_manager.py:** مدير محفظة ذكي ب Kelly Criterion
- **prediction_tracker.py:** قفل ذري (atomic lock) لمنع تلف البيانات بين البوتات
- **market_data.py:** أنبوب بيانات مشترك مع TTL Cache

---

## الدعم والتواصل

- **التنبيهات:** Telegram (4 بوتات)
- **لوحة التحكم:** localhost:5000
- **السجلات:** `advanced_bot.log`, `paper_pairs.json`

---

**آخر تحديث:** 2026-08-09
**الإصدار:** Phase 2.0 (Relative-Value Engine)
**الحالة:**  Paper Trading (أموال وهمية)
